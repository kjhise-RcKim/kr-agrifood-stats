# 농식품 주요통계 대시보드 — 디자인 리뉴얼 스펙 v1

> **이 문서의 용도**: Claude Desktop 코워크(소넷)에게 전달하는 구현 지시서입니다.
> `landing-mockup.html`과 함께 프로젝트 폴더에 넣고, 이 스펙대로 기존 코드에 적용을 요청하세요.
> **먼저 git 브랜치 `design-refresh`를 만들고 작업할 것.** main에 직접 커밋 금지.

---

## 0. 디자인 컨셉: "들녘의 새벽"

- 랜딩 히어로 = **어두운 이야기** (녹흑 배경 + 밀 골드 데이터 라인, 스크롤 스토리텔링)
- 대시보드 본문 = **밝은 도구** (옅은 웜그레이, 빠르고 기능적)
- 어두움→밝음의 전환 자체가 "이야기에서 도구로"라는 사이트 구조를 설명함
- 최대 강점인 신뢰 요소(근거 쪽수, 원자료 그대로)를 모노스페이스 배지로 일관되게 브랜딩

## 1. 컬러 토큰

### 라이트 모드 (본문 기본)
| 토큰 | 값 | 용도 |
|---|---|---|
| `--ink` | `#141F1A` | 본문 텍스트, 히어로/푸터 배경 |
| `--ink-soft` | `#2A3A32` | 보조 다크 표면 |
| `--paper` | `#F7F8F5` | 페이지 배경 |
| `--surface` | `#FFFFFF` | 카드 표면 |
| `--line` | `#E3E7E0` | 1px 보더 |
| `--wheat` | `#C9962E` | 시그니처 액센트: 데이터 라인, CTA, 로고 |
| `--wheat-soft` | `#E8C87A` | wheat hover |
| `--green` | `#1B6B4A` | 인터랙션 액센트: 링크, 포커스, 상승 델타 |
| `--green-tint` | `#EAF3EE` | green 배경 틴트 |
| `--muted` | `#6B776F` | 보조 텍스트 |
| 하락/주의 | `#B4552D` | 하락 델타 (빨강 대신 흙빛 오렌지 — 부정적 뉘앙스 완화) |

### 다크 모드 (기존 🌙 토글 유지)
- 배경 `#101713`, 표면 `#1A241E`, 보더 `#2A3630`, 텍스트 `#E8EAE5`, muted `#8B968E`
- `--wheat`, `--green`은 동일 유지 (다크에서 채도 잘 살아남음)
- 그래프 배경 그리드는 `rgba(255,255,255,0.06)`

### 차트 팔레트 (품목별 다중 선용, 순서 고정)
`#C9962E` (계/대표) → `#1B6B4A` → `#4A7FB5` → `#B4552D` → `#7A6AA8` → `#5B8A3C`
- "계(전체)" 선은 항상 wheat 골드 + 굵기 2.5px, 품목별 선은 1.5px

## 2. 타이포그래피

| 역할 | 폰트 | 로드 |
|---|---|---|
| UI/본문 | Pretendard Variable | jsdelivr CDN (목업 head 참조) |
| 헤드라인(display) | Noto Serif KR 600 | Google Fonts |
| 숫자·출처배지·축라벨 | IBM Plex Mono 400/500/600 | Google Fonts |

- **모든 수치에 `font-feature-settings: 'tnum'`** (고정폭 숫자) — body 전역 적용됨
- 타입 스케일: 12 / 13.5 / 15.5 / 17 / 22 / 28 / 34 / 44 / 64 (px, clamp로 반응형)
- 큰 숫자는 크게, 라벨은 작고 옅게 — 대비 극대화가 원칙

## 3. 아이콘: 이모지 전량 교체

**기존 이모지(📑⚖️📊ℹ️🌙⬇🔗💬⭐📄) → [Lucide](https://lucide.dev) 라인 아이콘으로 전량 교체.**
stroke-width 2, 크기 16~18px. 매핑:

| 기존 | Lucide |
|---|---|
| 📑 브리프 | `file-text` |
| ⚖️ 지표 비교 | `git-compare` 또는 `scale` |
| 📊 연도 비교 | `bar-chart-3` |
| ℹ️ 이용안내 | `info` |
| 🌙 다크토글 | `moon` / `sun` |
| ⬇ CSV/이미지 | `download` |
| 🔗 링크 복사 | `link` |
| 💬 AI 질문 | `message-circle` |
| ⭐ 즐겨찾기 | `star` |
| 📄 원문 p.XX | `file-text` (작은 배지 안) |

**예외**: 브리프 주제 이모지(🌾🛡️🍚🐄...)는 콘텐츠 성격이므로 유지 가능. 단 UI 컨트롤에서는 금지.

## 4. 시그니처 컴포넌트

### 4-1. 출처 배지 (p.XX) — 신뢰의 브랜딩
```
[file-text 아이콘] 원문 p.86
```
- IBM Plex Mono 11px, `--muted`, 1px `--line` 보더, radius 6px, 배경 `--paper`
- 모든 그래프·카드·AI 답변에 동일 스타일로 통일

### 4-2. 밀 이삭 로고 마크
- 목업의 `.brand-mark` SVG (골드+그린 밀 이삭) — 파비콘/OG 이미지에도 활용 가능

## 4-3. 첫 페이지 전체 구조 (기존 기능 전부 유지 · 재배치만)

목업이 첫 페이지 전체의 완성본입니다. 순서:

1. **히어로** (신규) — 밀 자급률 스토리텔링. 딥링크 진입 시 통째로 스킵
2. **전환부 + 통계 스트립** — 46 지표 · 12 브리프 · 581쪽 롤링 숫자
3. **검색** — 기존 통합검색(지표+원문) 그대로, 위치만 상단으로
4. **주목할 변화 TOP 5 / 내 관심지표** — 탭 전환. TOP5는 기존 자동계산 로직 그대로 연결(목업 수치는 예시). 관심지표 탭: 즐겨찾기 없으면 빈 상태 안내, 있으면 동일 행 스타일로 목록 + "겹쳐보기(첫해=100)" 버튼
5. **대표지표 11개** — 목업 카드 스타일로 전부 교체. "전체 지표" 진입점 유지
6. **주제별 브리프 12개** — 칩 그리드(이모지+이름+지표 수). 주제 이모지는 콘텐츠 성격이므로 유지
7. **신뢰 원칙 밴드 → 푸터**
8. **AI 플로팅 버튼** — 우하단 고정, hover 시 라벨 확장. 기존 챗봇 패널을 여기서 호출. 어느 화면에서든 표시

## 5. 랜딩 히어로: 스크롤 스토리텔링 (목업 그대로 이식)

- 구조: `hero-track`(높이 420vh) > `hero-sticky`(sticky 100vh)
- 스크롤 진행도(0→1)에 따라 SVG 라인이 `stroke-dashoffset`으로 그려짐
- 변곡점 4개에서 캡션 페이드인: 1970(15.4%) → 1984(수매 중단) → 1990~2000년대(0.x%) → 2020→2023(0.5→2.0%)
- 90% 지점에서 area gradient 페이드인
- **"건너뛰고 대시보드로" 링크 필수** (반복 방문자 배려)
- **직링크(지표/브리프 딥링크) 진입 시 히어로를 통째로 스킵**하는 기존 동작 유지
- `prefers-reduced-motion`: sticky 해제, 선/캡션 즉시 완성 상태 표시 (목업에 구현됨)

### ⚠️ 데이터 교체 필수
목업의 `WHEAT` 배열은 **중간 연도가 근사치**입니다. 프로젝트가 보유한 실제 밀 자급률
시계열로 반드시 교체할 것. 확인된 앵커: 1970=15.4 / 2020=0.5 / 2021=1.1 / 2022=1.3 / 2023=2.0.
캡션 문구의 수치도 실제 데이터와 대조해 수정.

## 6. 모션 스펙

| 요소 | 방식 | 타이밍 |
|---|---|---|
| 히어로 라인 | scroll-linked dashoffset | 진행도 ×1.08 (살짝 앞서감) |
| 캡션 | opacity+translateY 14px | 550ms ease |
| 롤링 숫자 | digit-reel translateY | 2200ms `cubic-bezier(0.16,1,0.3,1)`, 자릿수당 80ms 딜레이 |
| 카드 등장 | opacity+translateY 22px, stagger | 카드당 70ms 딜레이, IntersectionObserver threshold 0.15 |
| 카드 hover | translateY(-4px) + 스파크라인 재드로잉 | 250ms / 900ms |
| 내비 배경 | 다크 블러 ↔ 라이트 블러 전환 | 400ms, 라이트 존 진입 시 |

- 모든 모션은 `prefers-reduced-motion: reduce`에서 비활성 (전역 규칙 목업에 포함)
- 스크롤 리스너는 `{ passive: true }`, 무거운 계산 금지 (LCP/INP 보호)

## 7. 카드/검색 등 본문 컴포넌트

- 카드: `--surface` 배경, 1px `--line`, radius 16px, hover 시 -4px 부상 + 옅은 그린 보더
- 카드 구성: 카테고리(mono 소문자) → 제목(+ⓘ) → 큰 숫자(mono 28px) → 델타(▲그린/▼흙오렌지) → 스파크라인 → 출처 배지
- 검색: 640px 중앙, focus 시 그린 링 `0 0 0 3px rgba(27,107,74,0.12)`, `⌘K` 단축키 배지
- 신뢰 원칙 3항목 밴드: `--green-tint` 배경 (목업 참조)

## 8. 코워크(소넷) 작업 지시 순서 제안

1. `git checkout -b design-refresh`
2. 디자인 토큰(CSS 변수) 교체 → 전역 폰트 로드 추가
3. 이모지 → Lucide 아이콘 전량 교체
4. 카드/배지/검색 컴포넌트 스타일 적용
5. 히어로를 목업 구조로 이식 + **실제 데이터 배열 연결** + 딥링크 스킵 로직 연결
6. 다크모드 토큰 매핑 검증
7. 브랜치 푸시 → Vercel 프리뷰 URL에서 기존과 비교 검토
