# 진행상황 (이어서 작업용)

> 다음 세션 시작 시 이 문서 + `프로젝트_인수인계.md` + `지표_페이지_매핑.md`를 먼저 읽으면 바로 이어갈 수 있음.
> 최종 업데이트: 2026-07-17 (③ 챗봇 배포 완료 — 3개 층 전부 라이브)
>
> **🌐 라이브 주소: https://kr-agrifood-stats.vercel.app/**  (Vercel · GitHub Push 시 자동 재배포)
> 챗봇 키: Vercel 환경변수 `GEMINI_API_KEY`. ⚠️ 스크린샷 노출된 최초 키는 새 키로 교체 권장.

## 지금까지 한 것 ✅

1. **GitHub 연결 완료** — 폴더가 Git 저장소이고 GitHub Desktop으로 origin/main에 연결·백업됨.
   - 앞으로 작업 후: GitHub Desktop에서 `Summary 입력 → Commit → Push origin` 3단계 반복.
2. **원본 PDF 확보** — `data/2025 농림축산식품 주요통계.pdf` (581p). 텍스트 추출 가능(스캔본 아님).
   - 페이지 규칙: **PDF 페이지 = 인쇄 쪽번호 + 4**.
3. **목차 매핑** — `docs/지표_페이지_매핑.md`에 30개+비교 지표별 표 위치·페이지 정리.
4. **데이터 추출 (30개 지표)** — `data/indicators.json` = 대시보드·검색·AI가 공유하는 단일 원본.
   - 추출 스크립트: `data/extract_all.py` (다시 돌리면 재생성됨). 텍스트 캐시는 세션마다 `pdftotext -layout`로 재생성 필요(`/tmp/pages.json`).
   - 값은 원문과 대조 검증 완료.
5. **대시보드 v3** — `web/index.html` (+ `web/data.js` = indicators.json을 JS로 복사한 것).
   - 더블클릭하면 브라우저에서 바로 열림. 헤드라인 카드(전년比)+그룹 탭+시계열/막대 그래프+출처 표기.
6. **② 검색 기능 완료** — 상단 검색창. 이름·키워드·그룹·설명·단위를 대상으로 구조화 필터(공백=AND). 결과 카드에 그룹 태그. 미수록 지표 사유는 대시보드 하단에 표기.
7. **③-1 규칙기반 자동 인사이트 완료(v4)** — 지표 상세에 실제 수치로 추세·변화율(전체/최근5·10년)·최고최저·관련지표 추이를 자동 생성해 문장으로 표시. 무료·API키 불필요. 숫자 생성 없음(계산만).
8. **③-2 Gemini 챗봇 완료(v5)** — 대시보드 하단 채팅 UI + 백엔드 함수 `web/api/chat.js`(Gemini 호출, 키 숨김, 데이터 근거·출처인용·숫자생성 금지 가드).
9. **✅ 배포 완료 (라이브)** — Vercel에 배포됨(Root Directory=`web`, env `GEMINI_API_KEY`). 챗봇 실제 작동 확인: 없는 값은 "자료에 없습니다"로 정직 응답, 있는 값은 출처(장·페이지) 인용. **제품 3개 층(①대시보드 ②검색 ③AI) 전부 라이브.**

## 현재 데이터에 담긴 지표 (34개)

- **국내 26개**: 농림업 생산액, 경지면적, 농가호수·인구, 농림어업 GDP비중, 농가소득 / 식량·곡물 자급률, 쌀 생산·면적·1인당소비 / 농림수산품 PPI, 식료품 CPI, 농가교역조건 / 가축 사육마릿수, 육류 생산·자급률 / 채소·과실 생산 / 수출·수입·무역수지 / 임산물 생산액 / 어가소득 / 식품산업 시장규모 / 경영주 고령화율
- **남북한 4개**: 북한 인구·명목GNI·농가인구·무역총액
- **국제비교 4개**: 일본 곡물자급률, 대만 농가인구, 중국 농촌인구(시계열) + OECD 주요국 쌀 생산량 비교(막대). 추출: `data/extract_foreign.py`.

## 다음에 할 일 (우선순위)

1. **보류 지표 처리** — 아래는 본서 구조상 미완:
   - `meat_consumption_pc`(1인당 축산물소비), `timber_self_suff`(목재 자급률): 계산식/다른 표 필요.
   - `fishery_production`(어업 생산량), `return_farming`(귀농·귀촌): **본서에 표 없음** → 제외할지 통계청 등 별도 출처로 보강할지 **결정 필요**.
   - (선택) OECD 다품목 비교(밀·옥수수 등), 일본 상세 스냅샷(p524).
1-b. **챗봇 배포(사용자 작업)** — `docs/배포_가이드.md`대로 Gemini 키 발급 + Vercel 배포(Root Directory=`web`, env `GEMINI_API_KEY`). 배포하면 챗봇 켜짐.
2. **② 검색 고도화(선택)** — 이후 RAG(설명+원문 임베딩 자연어 질의)로 확장 가능.
3. **데이터 확장(사용자 요청 · 다음 우선)** — 품목별 세부 자급률 등 더 깊은 데이터 추가.
   - **품목별 자급률**: 현재는 '계(전체)'만 수록. 원자료에 쌀·보리쌀·밀·옥수수·두류·서류 등 품목별 자급률이 있음 (식량자급률 p.331 / 곡물자급률 p.330 — 각 표에 품목 열 존재). breakdown 구조로 추가하면 챗봇이 "밀 자급률?"에도 답 가능.
   - 그 외 후보: OECD 다품목 비교(밀·옥수수 등), 일본 상세 스냅샷(p.520~), 1인당 축산물 소비·목재 자급률(별도 계산).
   - 방법: `data/extract_all.py`에 breakdown 추가 → `python3 extract_all.py` → `web/data.js` 갱신 → Push(자동 재배포).

> 참고: 대시보드는 `compare` 필드가 있는 지표를 자동으로 가로 막대(국가별 비교)로 그림. 시계열은 `series`/`breakdown`.

## 이어서 작업하는 법 (다음 세션)

1. 이 폴더(`kr-agrifood-stats`)를 Cowork에서 다시 열기.
2. Claude에게 "이 프로젝트 이어서 하자, docs 읽어줘"라고 말하기.
3. 데이터 다시 뽑을 일 있으면: PDF에서 `pdftotext -layout`로 텍스트 뽑아 `python3 data/extract_all.py` 실행 → `web/data.js` 갱신.
4. 작업 후 GitHub Desktop에서 Commit → Push.

## 참고: 설계 원칙 (변경 금지)
대시보드·검색·AI가 **같은 JSON 하나**(indicators.json)를 읽는다. 지표 1개 = 레코드 1개. 모든 레코드에 `source`(장·페이지) 필수 → 출처표시 + AI 인용 근거. 실제 수치만 사용, 숫자 생성 금지.
